// Public surface of the self-contained core app. Both the standalone entry and
// the future embed shell import from here.
export { CoreApp } from './CoreApp';
export type { CoreAppConfig } from './CoreApp';
